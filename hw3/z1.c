#include <ctype.h>
#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <limits.h>
#include <string.h>
int duck(int x)
{
return x*x*x;
}
int main()
{
    int n, m, i, j;
    //scanf("%d", &n); 
    printf("%i\n", duck(5));
    //("%i\n", duck(n));
}